export enum HttpStatusCode {
  BadRequest = 400,
  InternalServerError = 500,
  Ok = 200,
  Unauthorized = 401,
}

export enum HttpResponseMessage {
  NewsItemAdded = "News Added!",
  InternalServerError = "Internal server error",
}
